{
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright install --with-deps; vitest"
  },
  "devDependencies": {
    "@vitest/browser": "^2.1.3",
    "playwright": "^1.48.0",
    "vitest": "^2.1.3"
  }
}
